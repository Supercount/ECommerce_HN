import { ArticlesTriPipe } from './articles-tri.pipe';

describe('ArticlesTriPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticlesTriPipe();
    expect(pipe).toBeTruthy();
  });
});
